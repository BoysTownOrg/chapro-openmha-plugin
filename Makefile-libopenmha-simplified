include ../../config.mk
include ../../magic.mk

libmha = lib$(MHATOOLBOX_NAME)

TARGETS=$(libmha).a

include ../../rules.mk

OBJECTS = \
	mha_parser.o mha_error.o mha_errno.o \
	mha_events.o mha_os.o

$(BUILD_DIR)/$(libmha).a: $(OBJECTS:%.o=$(BUILD_DIR)/%.o)
	$(COMPILERPREFIX)ar -rucs $$PWD/$@ $^
